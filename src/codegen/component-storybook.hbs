import React from 'react';
import { Story, Meta } from '@storybook/react/types-6-0';
import {
  BaseSection,
  ThemeNamesType,
} from '@applydigital/moderna-component-library';
import { {{pascalCase namespace}}, I{{pascalCase namespace}} } from '.';

export default {
  title: 'Components/{{pascalCase namespace}}',
  component: {{pascalCase namespace}},
  argTypes: {
    theme: {
      control: {
        type: 'select',
        options: ['blue', 'light'],
      },
    },
  },
} as Meta;

type {{pascalCase namespace}}StoryProps = I{{pascalCase namespace}} & {
  theme: ThemeNamesType;
};

const Template: Story = ({ theme, ...args }: {{pascalCase namespace}}StoryProps) => (
  <BaseSection theme={theme}>
    <{{pascalCase namespace}} {...args} />
  </BaseSection>
);

export const Default = Template.bind({});

Default.args = {
  // TODO: add default values here eg ...defaultData
  // TODO: remove this if theme prop exists in this component
  theme: 'light', // just for storybook
};
